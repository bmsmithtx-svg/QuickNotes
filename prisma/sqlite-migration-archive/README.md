# Archived SQLite Migrations

These migrations are preserved for reference only. They were used by the previous local SQLite development database and must not be executed against PostgreSQL or Supabase.

The active Prisma migration history is `../migrations`, which now contains only PostgreSQL-compatible migrations.
